// packages/web/tests/components/InvoiceViewer.test.tsx
import InvoiceViewer from '@/components/invoices/InvoiceViewer';
import { render } from '@testing-library/react';

describe('Component: InvoiceViewer', () => {
  it('should render the invoice viewer', () => {
    render(<InvoiceViewer />);
    // Test implementation will be added in a future step
  });
});
