// packages/web/tests/components/QuoteForm.test.tsx
import QuoteForm from '@/components/QuoteForm';
import { render } from '@testing-library/react';

describe('Component: QuoteForm', () => {
  it('should render the quote form', () => {
    render(<QuoteForm />);
    // Test implementation will be added in a future step
  });
});
